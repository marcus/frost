# Third-party data notices

- **models.dev** (https://models.dev): model metadata and provider pricing. The repository is MIT licensed; copyright the models.dev contributors. Recorded fixtures under `testdata/` are trimmed copies of `models.json` and `api.json`.
- **SWE-bench leaderboard** (https://www.swebench.com, data from https://github.com/SWE-bench/swe-bench.github.io): leaderboard JSON rows. Only submission folder IDs, scores, dates, agent names, and model tags are imported; no logs or trajectories.
- **Artificial Analysis** (https://artificialanalysis.ai): free-tier API data is for internal use with attribution and may not be redistributed. The producer writes it only to a `--restricted-out` catalog. The fixture under `testdata/` is synthetic and mirrors the response shape only.
